{
  "name": "svelte-summit-2020-oct",
  "version": "0.0.0",
  "scripts": {
    "dev": "run-p dev:routify dev:svite",
    "dev:routify": "routify run",
    "dev:svite": "svite",
    "build": "run-s build:routify build:svite",
    "build:routify": "routify run -b",
    "build:svite": "svite build --ssr"
  },
  "dependencies": {
    "css-media-vars": "^1.0.1",
    "svelte-css-vars": "0.0.1",
    "@roxi/routify": "^2.0.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "stylus": "^0.54.8",
    "svelte-preprocess": "^4.0.8",
    "svelte": "3.25.1",
    "svelte-hmr": "0.10.3",
    "svite": "^0.7.0"
  }
}